run:
  timeout: 5m
issues:
  exclude-dirs:
    - pkg/acme/x
    - pkg/common
linters:
  disable-all: true
  enable:
    - gofmt
    - goimports
    - gosimple
    - govet
    - ineffassign
    - misspell
    - staticcheck
    - typecheck
    - unconvert
